**Archivo: teacher-dashboard.html**

*   **Componente: Formulario de Creación de Examen (`#create-exam-form`)**
    *   **Hallazgo:** El formulario y sus elementos principales están correctamente definidos.
    *   **ID del Formulario:** `create-exam-form`
    *   **Contenedor de Preguntas:** Existe un `div` con ID `questions-container`, destinado a albergar las preguntas dinámicas.
    *   **Botón de Añadir Pregunta:** Existe un botón con ID `add-question-btn`, claramente identificado para añadir nuevas preguntas.
    *   **Botón de Guardar:** Existe un botón `type="submit"` dentro del formulario para guardar el examen.
    *   **Observación:** La estructura HTML está completa y es adecuada para la funcionalidad requerida. No se observan errores estructurales. La lógica faltante reside en el archivo JavaScript asociado.

**Archivo: js/teacher.js**

*   **Componente: Lógica de Creación de Examen**
    *   **Hallazgo 1 (Botón sin acción):** No existe un `addEventListener` para el elemento `add-question-btn`. Esto confirma por qué el botón no realiza ninguna acción al ser presionado.
    *   **Hallazgo 2 (Error al guardar):** El `addEventListener` para el evento `submit` del formulario `createExamForm` está presente, pero la lógica para recolectar los datos está incompleta. La variable `payload` se inicializa como un objeto vacío (`{}`) y se envía directamente a la API. Esto causa el error `forEach` en el backend, ya que el servicio espera un array de `preguntas` que no existe en el payload.
    *   **Observación:** El archivo está en el estado original reportado en la tarea, con la funcionalidad de añadir preguntas y guardar el examen rota.
